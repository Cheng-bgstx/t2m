# Text-to-Motion API Gateway Configuration

# Server settings
PORT=8080
HOST=0.0.0.0

# Remote WebSocket motion generation server settings
# This is the actual AI motion generation server (StableMoFusion or similar)
REMOTE_WS_HOST=127.0.0.1
REMOTE_WS_PORT=8000
REMOTE_WS_PATH=/ws

# Data management settings
# How long to keep motion data after last activity (minutes)
DATA_RETENTION_MINUTES=30
# How often to run cleanup (minutes)
CLEANUP_INTERVAL_MINUTES=5
# Maximum number of stored motions per user session
MAX_STORED_MOTIONS_PER_USER=10

# Rate limiting
MAX_REQUESTS_PER_MINUTE=10

# CORS settings (comma-separated list, or * for all)
ALLOWED_ORIGINS=*
# Example for specific origins:
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://yourdomain.com
